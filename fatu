"use client";

import { useState } from "react";
import { styles } from "../../styles/AppStyles";

export default function FatkaView() {
  const [cif, setCif] = useState("");
  const [server, setServer] = useState("O");
  const [response, setResponse] = useState(null);
  const [error, setError] = useState("");

  const BASE_URL = process.env.NEXT_PUBLIC_API_URL;

  const handleSubmit = async (event) => {
    event.preventDefault();
    setError("");
    setResponse(null);

    // ✅ Strict 11-digit validation
    const regex = /^\d{11}$/;
    if (!regex.test(cif)) {
      setError("CIF must be exactly 11 digits");
      return;
    }

    const userToken = localStorage.getItem("userToken");
    const url = `${BASE_URL}/fatka`;

    try {
      const res = await fetch(url, {
        method: "POST",
        headers: {
          "Content-Type": "application/json",
          Authorization: `Bearer ${userToken}`,
        },
        body: JSON.stringify({
          cif: cif,
          server: server,
        }),
      });

      const data = await res.json();

      if (!res.ok) {
        throw new Error(data?.detail || "Something went wrong");
      }

      setResponse(data);

    } catch (err) {
      console.error("Error:", err);
      setError(err.message);
    }
  };

  return (
    <div>
      {/* Page Header */}
      <div style={styles.pageHeader}>
        <div>
          <h1 style={styles.pageTitle}>FATKA Enquiry</h1>
          <p style={styles.pageSubtitle}>
            Perform FATKA check in selected environment
          </p>
        </div>
      </div>

      {/* Form Card */}
      <div style={styles.formCard}>
        <div style={styles.formGrid}>

          {/* CIF */}
          <div style={styles.formGroup}>
            <label style={styles.label}>CIF Number</label>
            <input
              type="text"
              value={cif}
              onChange={(e) => {
                const digits = e.target.value.replace(/\D/g, "");
                if (digits.length <= 11) {
                  setCif(digits);
                }
              }}
              style={styles.input}
              placeholder="Enter 11-digit CIF"
            />
          </div>

          {/* Server */}
          <div style={styles.formGroup}>
            <label style={styles.label}>Server</label>
            <select
              value={server}
              onChange={(e) => setServer(e.target.value)}
              style={styles.select}
            >
              <option value="O">O - UAT</option>
              <option value="X">X - UAT</option>
              <option value="Y">Y - UAT</option>
              <option value="Z">Z - UAT</option>
              <option value="K">K - Pre-Prod</option>
              <option value="J">J - Pre-Prod</option>
              <option value="R1">R1 - Pre-Prod</option>
              <option value="Y2">Y2 - Pre-Prod</option>
              <option value="L">L - Pre-Prod</option>
            </select>
          </div>

        </div>

        {error && (
          <div style={styles.errorMsg}>
            {error}
          </div>
        )}

        <button
          onClick={handleSubmit}
          style={styles.primaryButton}
        >
          Run FATKA
        </button>
      </div>

      {/* Results Section */}
      {response && (
        <div style={styles.resultsCard}>
          <div style={styles.resultsHeader}>
            <h3 style={styles.resultsTitle}>FATKA Result</h3>
          </div>

          <div style={styles.resultsList}>
            <div style={styles.resultItem}>
              <div style={styles.resultNumber}>1</div>
              <div style={styles.resultContent}>

                <div>
                  <span style={styles.resultLabel}>Server:</span>
                  <span style={styles.resultValue}>
                    {response.server}
                  </span>
                </div>

                <div>
                  <span style={styles.resultLabel}>Extracted Data:</span>
                  <span style={styles.resultValue}>
                    {response.extracted || "N/A"}
                  </span>
                </div>

                <div>
                  <span style={styles.resultLabel}>Server Response:</span>
                  <span style={styles.resultValue}>
                    {response.server_response}
                  </span>
                </div>

              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}